# AI Bot Coder Agent

This repository contains both the frontend (VS Code extension) and the backend (Flask server) for the AI Bot Coder Agent.

## Folder Structure
- `/frontend`: VS Code Webview extension built with Javascript and HTML.
- `/backend`: Flask server orchestration layer supporting local Ollama models (`gemma4`) and LLM7 APIs.
